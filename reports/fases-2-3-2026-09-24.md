# Fases 2 y 3 sin Felipe: reporte del 2026-09-24 (día)

Objetivo: hacer todo lo de las Fases 2 y 3 que no dependa de Felipe. Resultado: la Fase 2 queda completa salvo lo que necesita material nuevo (clip HDR real, 5 clips anotados), y la Fase 3 queda hecha en biblioteca de B-roll propio, transiciones, SFX, música, tarjeta de cierre y el segundo cerebro. Lo que sigue pendiente es el gate subjetivo de estilo y las tres cosas que solo Felipe puede aportar. 17 commits locales nuevos en `main`, sin push.

## Corridas del día (mismo brief, mismo clip por fila)

| Métrica | Corrida 7 (Claude, es-MX) | Corrida 8 (Claude, es-MX, final) | Codex (IMG_1778, paridad) |
|---|---|---|---|
| Duración final | 48.3 s | 48.3 s | 36.2 s |
| B-roll | 3 de stock | 2 de la biblioteca propia + 4 de stock | no aplicaba |
| Gráficos | 6 | 8, con tarjeta de cierre | 4 |
| Música | no | sí, CC BY con crédito, ducking | no (brief) |
| Palabras off-mic que quedaron | 0 | 0 | 0 |
| Cortes por segundos | 0 | 0 | 0 |
| Rechazos de esquema | 1 | 2 | 0 |
| Errores de herramienta | 2 | 3 | 0 |
| Intentos de shell | 0 | 0 | 0 |
| Avisos de validate al final | 0 | 1 | 0 |
| Turnos, minutos, costo | 50, 14.5, $1.32 | 57, 10.1, $1.39 | 65 llamadas, 6.6, plan ChatGPT |
| Render final con QC | pasó (2.º intento) | pasó: −14.3 LUFS, −1.3 dBTP | borrador |

Notas de lectura. En la corrida 8 el agente se quedó en el borrador y no hizo el render final; lo cerré yo por el MCP con el whip al sky bar (que su herramienta aún no aceptaba sobre un cue de B-roll) y SFX. Los tres errores de la 8 ya no se repiten: `label` en la etiqueta de ubicación ahora es alias de `place`, el CTA admite 36 caracteres, y los cues de B-roll aceptan transiciones. La medida de Codex no incluye costo porque corre con la suscripción de ChatGPT; consumió 1.09 M tokens de entrada, casi todos cacheados.

Métricas completas: `.captions-tmp/run{7,8}-metrics.json` y `.captions-tmp/codex-run2-metrics.json` (`scripts/run-report.mjs` ya lee los dos formatos).

## Codex como segundo cerebro

- `scripts/codex-edit.sh` corre `codex exec --json --ephemeral --ignore-user-config -s read-only` con el servidor `reel` en la línea de comandos y pre-aprobado (`mcp_servers.reel.default_tools_approval_mode=approve`, `approval_policy=never`). Sin esa clave Codex 0.156 rechaza toda llamada MCP en modo no interactivo; `--approve-for-me` las aprueba pero deja escribir en el repo (probado con un `touch`), así que no se usa. No toca `~/.codex/config.toml`.
- Codex recibe las imágenes de `caption_proof`: reaccionó al contact sheet moviendo captions que cruzaban la línea del cabello. Siguió la skill completa: idioma, off-mic en `cut`, brand kit guardado, candidatos de corte en una llamada, autocut después de cortar (10 clips limpios, sin el bug de los 143), captions `prism`, gancho, tres etiquetas, `prepare_mattes`, borrador.
- Diferencias frente a Claude: quitó la palabra detrás de la persona cuando `validate` avisó que la cabeza la tapaba, y aplicó una cámara lenta de 0.95× para llegar al mínimo de 35 s. Lo segundo es discutible y conviene prohibirlo en la skill.
- `test/runner.test.mjs` cubre los dos runners.

## Entregables (commits)

**Fase 2**
- `0474383` Música CC0/CC BY desde Openverse con crédito (`search_music`, `set_music music_id`), limpieza de voz opcional (`set_audio clean`: el piso de ruido de un extracto de teléfono baja de −41 a −59 dB con la voz intacta), aviso `cut-word` en `validate`, test de aislamiento del runner.
- `cbce427` `search_music` reintenta con menos palabras y acepta loops; el render repite la pista si es más corta que el reel.
- `a30c391` El loudnorm mide el archivo codificado y baja el objetivo hasta pasar el gate (la fuente de `pruebaeditoria` satura).

**Fase 3**
- `562f413` Biblioteca de B-roll propio: `add_broll_assets` con contact sheet, `tag_broll_asset`, `broll_library`, `suggest_broll` (coloca por palabra con las reglas de los editores y exige cubrir el negro; en `pruebaeditoria` el negro de 7 a 40 s sale como seis colocaciones contiguas). `add_broll` acepta `asset_id` y `at_wid`.
- `b227cd0` Transiciones `card` y `split` con capa inferior, SFX sintetizados con ffmpeg (nada que licenciar), `set_speed_ramp` por pasos.
- `7f05a97` Los cues de B-roll aceptan `punch`, `zoom` y `whip` con su whoosh.
- `bf6362a` Runner de Codex; `f819c53` `run-report.mjs` lee sus logs; `204eaf2` estado de los criterios de la Fase 3 en PLAN.md.

**Arreglos que salieron de las corridas**
- `d219e15` Los captions se apartan solos de los gráficos de texto (el hook sobre el primer caption de la corrida 6).
- `1480a4b` Placa de vidrio en las etiquetas de dos tonos.
- `65e61fb` Los archivos de Pexels se nombran por hash de la URL: dos cues de la corrida 7 compartían archivo y el "sky bar" era el salón.
- `d502e65`, `8c6c63b`, `a9d2220` Alias de props y límite del CTA que hicieron perder turnos.

## Incidentes

- **El venv de WhisperX desapareció.** `.venv` apuntaba a `~/autobroll/.venv` y esa carpeta ya no existe (se borró al liberar disco). Las dos corridas de la mañana funcionaron porque los transcripts están cacheados; cualquier clip nuevo habría fallado. Lo reconstruí con `npm run setup -- --cpu` (Python 3.10, torch CPU, WhisperX, OpenCV, MediaPipe); `health` vuelve a estar en verde y los mattes de la corrida de Codex ya usaron el venv nuevo. Quedan 6.5 GB libres.
- **Puerto 3333 compartido.** El `config.toml` de Codex del usuario tiene un servidor MCP `brilliant` en `http://127.0.0.1:3333/mcp`, el mismo puerto del backend de reel-agent. No chocó hoy, pero conviene mover uno de los dos.

## Lo que se ve en los frames y aún no está bien

1. **La tarjeta de cierre inventó un handle** (`@montalban326`) porque el brief no dio ninguno. La skill ahora pide dejarlo vacío si no viene en el brief.
2. **Los captions `prism` sueltos** ("Es", "de un") aparecen aislados en pantalla cuando la página se construye palabra por palabra sobre B-roll oscuro; se leen pero se sienten pobres. Falta una regla de página mínima o un contenedor para ese estilo sobre B-roll.
3. **El B-roll propio del salón** muestra el techo con el climatizador: es el material que hay; con más metraje del cliente `suggest_broll` lo elegiría mejor.

## Bloqueado (depende de Felipe)

- Capturas reales de las safe zones de Reels; export de Captions.ai para el gate subjetivo; un clip HDR real de iPhone; 5 clips con retomas y muletillas anotadas a mano para medir recall; la señal de boca (MediaPipe crashea en esta Mac); una línea final regrabada para IMG_1778.

## Archivos

- Corrida 8 final: `public/exports/edited-1790270211345.mp4` (77 MB); frames en `.captions-tmp/frames-run8/grid.jpg`.
- Codex: `public/exports/edited-1790269477263-draft.mp4`; frames en `.captions-tmp/frames-codex/grid.jpg`.
- Logs: `.captions-tmp/agent-run{7,8}.jsonl`, `.captions-tmp/codex-run{1,2}.jsonl`.
